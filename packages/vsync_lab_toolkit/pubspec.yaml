name: vsync_lab_toolkit
description: Reusable Flutter frame timing monitor and frame log exporter from VSync Lab.
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
